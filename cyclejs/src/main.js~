import {Observable} from 'rx';
import Cycle from '@cycle/core';
import {div, button, p, makeDOMDriver} from '@cycle/dom';
import makeGridDriver from 'GridDriver';

function main(sources) {
    sources.GridDriver 
    
    return {
	GridDriver: 
    };
}

Cycle.run(main, {
    GridDriver: makeGridDriver("#canvas")
});
